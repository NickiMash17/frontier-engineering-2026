# Evaluation

This document is the authoritative description of the Tier A benchmark,
its ground truth, the methodology, and the results of the one full run
performed so far. Every number below traces to
`evaluation/results/2026-08-29T05-28-25-444Z/results.json`, generated by
`node scripts/evaluate.js`.

## Benchmark: Tier A (controlled fixtures)

Four self-authored `fixtures/*` repositories, real npm-installed packages
at real CVE-affected versions, each isolating one reachability scenario.
CVE and version data is real, pulled from OSV.dev, and where a version's
actual exploitability was not obvious from advisory metadata alone, it
was independently confirmed by running the real exploit payload against
the real installed package (see `docs/EXPERIMENT_LOG.md` Experiment 1).

| Case | CVE | Package@version | Scenario | Ground truth |
|---|---|---|---|---|
| `case-a-reachable-minimist` | CVE-2020-7598 | `minimist@1.2.0` | HTTP handler passes request-body array directly into `minimist()` | `REACHABLE` |
| `case-b-unreachable-minimist` | CVE-2020-7598 | `minimist@1.2.0` | Only used by a dev-only build script; server never imports it | `NOT_REACHABLE` |
| `case-c-condition-not-satisfied-lodash` | CVE-2019-10744 | `lodash@4.17.11` | `_.defaultsDeep()` reached by attacker input, but a `sanitize()` step strips the required `__proto__`/`constructor`/`prototype` keys first | `CONDITION_NOT_SATISFIED` |
| `case-d-indirect-minimist` | CVE-2020-7598 | `minimist@1.2.0` | Reachable only via one hop of indirection through a local wrapper module | `REACHABLE` |

Ground truth for every case is hand-authored in that case's
`ground_truth.json`, written before the advanced system was run against
it, and never adjusted after seeing the agent's output (Experiment 1's
correction was to the *fixture's chosen CVE/version*, made by independent
empirical testing, before the fixture was considered final -- not a
post-hoc adjustment to match an answer).

## Methodology

For each case, `scripts/evaluate.js`:
1. Runs `scripts/baseline.js` -- a deterministic, code-blind version-range
   match against `data/advisories/index.json` (itself pulled from real
   OSV.dev records, see `data/advisories/*.json`).
2. Runs the advanced agent (one headless Claude Code CLI call per case,
   `Read`/`Grep`/`Glob` only -- see `docs/ARCHITECTURE_DECISIONS.md`
   ADR-001) with the baseline's flagged CVE/package/version as context.
3. Compares both outputs against `ground_truth.json` -- the advanced
   agent's own stated `confidence` is recorded but never substituted for
   this comparison.
4. Independently re-verifies every cited `evidence[].file`/`lines` entry
   against the real filesystem (existence + line-count bounds) before
   counting it as valid evidence -- the agent's claim that a citation is
   accurate is not trusted on its own.

## Metrics

- **Risk-classification accuracy**: does the system correctly separate
  `REACHABLE` (real risk) from everything else (`NOT_REACHABLE`,
  `CONDITION_NOT_SATISFIED`, `UNCERTAIN` -- not an actionable risk)?
- **Exact verdict accuracy**: does the full 4-way verdict match ground
  truth exactly (only meaningful for the advanced system -- the baseline
  only ever outputs a binary `VULNERABLE`/`NOT_VULNERABLE`)?
- **False positives / false negatives**: on the risk-classification
  question above.
- **Evidence completeness**: fraction of cited evidence entries that
  independently verify (see Methodology step 4).
- **Cost / latency**: taken directly from the CLI's own reported
  `total_cost_usd` / `duration_ms`, not separately estimated.

## Results

| Metric | Baseline | Advanced |
|---|---|---|
| Risk-classification accuracy | 2/4 = 50% | 4/4 = 100% |
| Exact verdict accuracy | n/a (binary only) | 4/4 = 100% |
| False positives | 2 | 0 |
| False negatives | 0 | 0 |
| Evidence completeness (avg) | n/a | 4/4 = 100% |
| Total cost (4 cases) | $0 | $0.2636 |
| Avg cost per case | $0 | ~$0.066 |
| Avg latency per case | ~0 ms | ~30,152 ms |

**Reading these numbers honestly:** n=4 is a Tier A controlled benchmark,
not the ~15-20-case real-world Tier B benchmark committed to in
`docs/PROJECT_SELECTION.md`. A 100% score on four cases is evidence that
the core mechanism works, not evidence that it generalizes -- Tier B,
with real repos and genuinely ambiguous cases, is the next test and is
expected to surface failures Tier A could not (see
`docs/DAY1_IMPLEMENTATION_PLAN.md` Section 9 for what was predicted to
fail here and didn't, and `docs/ARCHITECTURE_DECISIONS.md` ADR-003 for
what that does and doesn't justify).

The baseline's two false positives (`case-b`, `case-c`) are the concrete,
measured version of the project's core claim: a conventional
version-match scanner flags both, and both are not actionable risks in
this application's actual code.

## Benchmark: Tier B (real-world CVE/repository pairs)

**Status: executed.** Every number below traces to
`evaluation/results-tier-b/2026-08-29T13-12-24-798Z/results.json`,
generated by `node scripts/evaluate_tier_b.js`. See
`docs/EXPERIMENT_LOG.md` Experiment 5 for the debugging arc that preceded
this run (three Windows-specific bugs in the CLI-invocation path, plus one
evaluator crash, found and fixed before a real run was possible) and
`docs/TIER_B_REPORT.md` for the full report.

5 hand-verified real cases, all from one pinned commit of
OWASP/NodeGoat (a real, intentionally realistic Express/MongoDB/Swig
application built for security education), frozen in
`evaluation/benchmarks/tier-b/manifest.json`:

| Case | CVE | Package@version | Expected verdict | Difficulty categories |
|---|---|---|---|---|
| `tb-1-underscore-dead-import` | CVE-2021-23358 | `underscore@1.9.1` | `NOT_REACHABLE` | installed-but-unreachable, dead path |
| `tb-2-marked-sanitize-bypass` | CVE-2016-10531 | `marked@0.3.5` | `REACHABLE` | direct reachable, indirect, sanitization/precondition, ambiguous, challenging |
| `tb-3-marked-redos-same-sink` | GHSA-p9wx-2529-fp83 | `marked@0.3.5` | `REACHABLE` | direct reachable, indirect (lower-confidence, see manifest notes) |
| `tb-4-lodash-pure-transitive` | CVE-2019-10744 | `lodash@4.17.11` | `NOT_REACHABLE` | transitive dependency, installed-but-unreachable |
| `tb-5-minimist-pure-transitive` | CVE-2020-7598 | `minimist@0.0.10` | `NOT_REACHABLE` | transitive dependency, installed-but-unreachable |

Two difficulty categories (reachable-but-condition-absent as a standalone
case, and attacker-control-distinction) are explicitly documented as gaps
in the manifest rather than force-filled -- see `manifest.json`'s
`documented_gaps`.

Methodology matches Tier A (Phase-by-phase account in
`docs/TIER_B_REPORT.md`): ground truth authored and reviewed before any
system was run against it, every citation checked against the real pinned
source, and where the underlying mechanism mattered for the verdict (the
two `marked` cases), it was independently exercised against the real
installed package rather than trusted from advisory text alone -- except
where doing so would mean constructing something closer to a working
exploit than a mechanism check, which was avoided per this benchmark's
own instructions.

### Results

| Metric | Baseline | Advanced |
|---|---|---|
| Accuracy | 2/5 = 40% | 5/5 = 100% |
| False positives | 3 | 0 |
| False negatives | 0 | 0 |
| Avg confidence | n/a | 0.89 |
| Avg evidence completeness | n/a | 86.5% |
| Total cost (5 cases) | $0 | $0.5680 |
| Avg cost per case | $0 | $0.1136 |
| Avg duration per case | ~0 ms | ~54 s |

Per-case:

| Case | Ground Truth | Baseline | Advanced | Correct? | Evidence Completeness | Duration | Cost |
|---|---|---|---|---|---|---|---|
| `tb-1-underscore-dead-import` | `NOT_REACHABLE` | `VULNERABLE` | `NOT_REACHABLE` | yes | 83% | 54s | $0.1106 |
| `tb-2-marked-sanitize-bypass` | `REACHABLE` | `VULNERABLE` | `REACHABLE` | yes | 100% | 44s | $0.0907 |
| `tb-3-marked-redos-same-sink` | `REACHABLE` | `VULNERABLE` | `REACHABLE` | yes | 100% | 40s | $0.0787 |
| `tb-4-lodash-pure-transitive` | `NOT_REACHABLE` | `VULNERABLE` | `NOT_REACHABLE` | yes | 71% | 44s | $0.1124 |
| `tb-5-minimist-pure-transitive` | `NOT_REACHABLE` | `VULNERABLE` | `NOT_REACHABLE` | yes | 78% | 87s | $0.1756 |

Baseline flagged all 5 cases `VULNERABLE` (it is code-blind by design);
3 of those 5 are false positives against actual reachability, the same
pattern Tier A demonstrated at smaller scale.

**Evidence-quality caveat:** one entry in `tb-1`'s evidence was marked
unverified by the automated checker not because it was wrong, but because
the agent cited a search method ("repo-wide grep for
`require('underscore')`") rather than a file+line location -- the checker
can only verify file-based citations. This is an evaluator limitation,
not an agent error, and is the reason `tb-1`'s completeness reads 83%
instead of 100% despite a correct, well-reasoned verdict.

**Reading these numbers honestly:** all 5 verdicts were exact matches --
zero reasoning failures were observed. But the two hardest patterns the
original plan predicted would be the real test of generalization
(a sanitization bypass where the mitigation is genuinely present just
insufficient, and framework-mediated indirect flows) were only exercised
by `tb-2`/`tb-3` in a single pairing, not stressed further. A clean 5/5 is
real evidence the mechanism holds beyond Tier A's controlled fixtures; it
is not evidence that it holds at every level of real-world difficulty --
see `docs/TIER_B_REPORT.md` Section 7 for what this does and doesn't
license concluding.
